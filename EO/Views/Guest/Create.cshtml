@model EO.Models.CreateGuestDto

@{
    ViewData["Title"] = "Edit Guest";
    var id = ViewBag.GuestId;
}

<link href="~/css/memberSetup.css" rel="stylesheet" />

<div class="mf-page">

    <div class="mf-card">

        <div class="mf-heading">Add Guest</div>
        <div class="mf-subtitle">Add guest information</div>

        <!-- Single Step -->
        <!-- <div class="mf-stepper">
            <div class="mf-stepper-line"></div>

            <div class="mf-step-item">
                <div class="mf-step-circle active">1</div>
                <div class="mf-step-label active">Guest Details</div>
            </div>
        </div>

        <div class="mf-progress-track">
            <div class="mf-progress-fill" style="width:100%;"></div>
        </div>

        <div class="mf-section-row">
            <div class="mf-section-title">Guest Information</div>
            <div class="mf-section-count">Single Step Form</div>
        </div> -->

        <form asp-action="Edit" asp-route-id="@id" method="post">

            <label>Name</label>
            <input asp-for="Name" />

            <label>Designation</label>
            <input asp-for="Designation" />

            <label>Avatar URL</label>
            <input asp-for="Avatar" />

            <div class="mf-btn-row">

                <a asp-action="Index"
                   class="mf-btn-back text-decoration-none">
                    Cancel
                </a>

                <button type="submit"
                        class="mf-btn-submit">
                    Update Guest
                </button>

            </div>

        </form>

    </div>

</div>